name: Open Journals PDF Generator

on: [push]

jobs:
  paper:
    runs-on: ubuntu-latest
    name: Paper Draft
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Build draft PDF
        uses: openjournals/openjournals-draft-action@master
        with:
          journal: joss
          paper-path: paper/paper.md
      - name: Upload
        uses: actions/upload-artifact@v1
        with:
          name: paper
          # This is the output path where Pandoc will write the compiled
          # PDF. Note, this should be the same directory as the input
          # paper.md
          path: paper/paper.pdf

# My own. It's way too slow installing the tex packages (> 10 mins)
#name: Open Journals PDF Generator
#
#on: [push]
#
#jobs:
#  paper:
#    runs-on: ubuntu-latest
#    name: Paper Draft
#    steps:
#      - name: Checkout
#      - uses: actions/checkout@v3
#      - name: Install Dependencies
#        run: |
#          sudo apt-get update && sudo apt-get install -y pandoc pandoc-citeproc texlive texlive-latex-extra texlive-science texlive-publishers
#      - name: Compile PDF
#        run: |
#          pandoc --filter pandoc-citeproc --bibliography=paper/paper.bib -s paper/paper.md -o paper/paper.pdf --template paper/latex.template
#      - name: Upload PDF
#      - uses: actions/upload-artifact@v3
#        with:
#          name: joss-paper
#          path: paper/paper.pdf
